[package]
name = "spark-macros"
version = "0.1.0"
edition.workspace = true
rust-version = "1.89"
authors = ["Gemini 架构组 <architecture@gemini.example>"]
license = "Apache-2.0"
description = "Spark 框架的过程宏集合，提供 service 派生等开发体验增强。"

[lib]
proc-macro = true
bench = false
test = false

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true }
